// has a function "operate" that takes an expression??? or a 3 value FORMAT: * operand,operator,operand
// If we pressed on a operator, it should process the 2 number first (if available) and display and save the result before continuing
// MUST!
   // Displays an error if 1 operand is the only input
   // Can only input 1 decimal, Disable after pressing for the first time 

